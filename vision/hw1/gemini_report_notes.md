2. 단계별 코드 동작 설명문제 1: Image TransformationsTranslation: $2 \times 3$ 행렬 $\begin{bmatrix} 1 & 0 & tx \ 0 & 1 & ty \end{bmatrix}$을 사용하여 픽셀의 위치를 $x, y$만큼 평행 이동시킵니다.Rotation: cv2.getRotationMatrix2D는 회전 중심, 각도, 배율을 입력받아 아핀 변환 행렬을 생성합니다.Affine: 선의 평행성은 유지되지만 길이나 각도가 변할 수 있는 변환으로, 3개의 대응점(Source-Target)이 필요합니다.Perspective: 원근감을 표현하는 $3 \times 3$ 변환으로, 4개의 대응점이 필요하며 warpPerspective를 사용합니다.문제 2: Filters & NoiseGaussian: 픽셀 주변에 가중치를 두어 부드럽게(Blur) 만듭니다. 고주파 성분(노이즈)을 제거하는 데 효과적입니다.Sobel/Laplacian: 픽셀 값의 변화(기울기)가 급격한 부분인 에지를 검출합니다. Sobel은 1차 미분, Laplacian은 2차 미분 개념을 사용합니다.Salt & Pepper Noise: 픽셀 값이 0(검정) 또는 255(흰색)로 튀는 현상입니다. Gaussian Filter는 이를 평균화시켜 잔상이 남지만, Median Filter는 정렬을 통해 극단값을 제거하므로 훨씬 탁월한 복원력을 보입니다.문제 3: Image PyramidsInterpolation: INTER_NEAREST는 속도가 빠르나 계단 현상이 있고, INTER_CUBIC이나 INTER_LANCZOS4는 연산량이 많지만 가장 부드러운 결과를 얻을 수 있습니다.Laplacian Pyramid: 가우시안 피라미드의 현재 층과 상위 층을 확대한 이미지 사이의 차이(이미지 디테일)를 저장합니다. 이를 통해 손실 없이 이미지를 재구성할 수 있습니다.문제 4: Manual Median Blur입력 이미지의 각 픽셀을 순회하며 정의된 커널 크기(size)만큼 영역을 잘라냅니다.해당 영역의 픽셀값들을 일렬로 세워 정렬한 뒤, 정중앙에 위치한 값을 결과 픽셀값으로 채택합니다. 이는 cv2.medianBlur의 내부 동작과 일치합니다.